import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pancakesandpixels.budget',
  appName: 'Budget Tracker',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
